import {
  selectTournamentListContainer,
} from '../selectors';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = selectTournamentListContainer();

describe('selectTournamentListContainer', () => {

});
